export type Experience = {
    id: number,
    name: string,
    sort: number,
    timeline: string,
    roles: string[],
    company: string,
    description: string,
    tags: string[],
    visible: boolean,
}